@model IEnumerable<DATN.Models.Amenity>

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Trang tiện nghi</h1>

<p>
    <a asp-action="Create" class="btn btn-primary"><i class="fa-solid fa-book"></i>Thêm tiện nghi</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>STT</th>
            <th>Tên tiện nghi</th>
            <th>Hành động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Select((value, index) => new { value, index }))
        {
            <tr>
                <td>@(item.index + 1)</td> <!-- Display Serial Number -->
                <td>
                    @Html.DisplayFor(modelItem => item.value.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.value.ID" class="btn btn-primary">Sửa</a>
                    <a asp-action="Delete" asp-route-id="@item.value.ID" class="btn btn-danger">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>
